# App Module

The core of the FastAPI backend application.

## Structure
- `main.py`: FastAPI app initialization and route registration.
- `schemas/`: Pydantic models for strict input/output validation.
- `services/`: Core logic including OCR extraction, deterministic math, and RAG retrieval.
- `core/`: Environment variables, configuration, and security implementations.
